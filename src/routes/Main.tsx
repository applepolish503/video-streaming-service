import React from "react";

export function Main(): JSX.Element {
  return (
    <div style={{ display: "grid", gap: 16 }}>
      <h1 style={{ fontSize: 28, margin: 0 }}>メイン画面</h1>
      <p style={{ color: "#666", marginTop: 0 }}>ここに世界地図、KPI、画面遷移ボタンなどを配置していきます。</p>
      <div style={{ padding: 16, border: "1px dashed #bbb", borderRadius: 8, background: "#fafafa" }}>
        プレースホルダー
      </div>
    </div>
  );
}


